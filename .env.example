# Variables de entorno para producción
# NO SUBIR ESTE ARCHIVO A GIT
# Copiar este archivo a .env y configurar con valores reales

# Seguridad (generar con: python -c "import secrets; print(secrets.token_urlsafe(50))")
SECRET_KEY=tu_clave_secreta_generada_aqui
DEBUG=False

# Configuración de Oracle Database
DB_NAME=localhost:1521/XEPDB1
DB_USER=django_user
DB_PASSWORD=tu_contraseña

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1,tu-dominio.com
